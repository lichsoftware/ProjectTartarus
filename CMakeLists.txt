cmake_minimum_required(VERSION 3.25)
project(ProjectTartarus)
set(CMAKE_C_STANDARD 11)

message(STATUS "-----------------------------------------------------")
message(STATUS "Generating for directory: ${CMAKE_CURRENT_SOURCE_DIR}")

add_subdirectory(PT)
